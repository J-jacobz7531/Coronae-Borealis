version: '3.8'

services:
  molstar-viewer:
    build: .
    container_name: molstar-viewer
    restart: unless-stopped
    ports:
      - "8080:3000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=${MONGODB_URI}